import styles from "./styles.module.less";

export default function AppFooter() {
  return (
    <footer className={styles.footer}>
      <span className={styles.footerText}>power by altria group</span>
    </footer>
  );
}
